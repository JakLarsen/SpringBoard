def adder(x,y):
    """
    Adds two numbers together
    
    Examples: 

    >>> adder(2,5)
    7
    >>> adder(-1,50)
    49

    """
    
    return x + y

#doctests generally used to show functionality, not full testing

#use> python -m doctest filename.py <to run
#use> python -m doctest -v filename.py <to run with verbose